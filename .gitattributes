# 批处理文件一律用 CRLF 落盘。
# cmd.exe 对 LF 换行的 .bat 支持不可靠（带 goto/label 的块会直接错），
# 而这几个 bat 是给别人双击用的入口，不能挑环境。
*.bat text eol=crlf

# 图片按二进制处理，别让 git 去动换行或做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
