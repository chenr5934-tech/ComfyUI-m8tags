# 本机配置（每台机器路径都不同，不要提交）
config.json

# 内置站点（atlas/）里的「大件」不提交：
#   images/      词库配图，1.6 GB / 44984 个文件，各人按需自己放
#   self-image/  我的图库（自己上传的图 + 索引 + 删除流水）
# 站点代码与 data/（词库数据）是内置的，随仓库一起分发。
atlas/images/*
!atlas/images/README.txt
atlas/self-image/*
!atlas/self-image/.gitkeep
# 初始空索引要跟着仓库走：站点 index.html 里有一条 <script src="self-image/index.js">，
# 文件不存在时页面会顶一条"脚本没加载成功"的红横幅（而且把人往浏览器缓存上引）。
# 后端虽然会兜底返回空索引，但 file:// 直接打开时没有后端，只能靠这个文件在。
# 用户存图后这个文件会被重写 —— 那是他自己的图库数据，不该提交，留意别误 add。
!atlas/self-image/index.js

# 运行时产物
cache/
__pycache__/
*.pyc
*.pyo

# 站点 e2e 脚本跑完留下的截图
_shot*.png

# 编辑器 / 系统
.venv/
venv/
.vscode/
.idea/
node_modules/
.DS_Store
Thumbs.db
